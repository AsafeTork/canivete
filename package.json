{
  "name": "canivete",
  "version": "1.0.0",
  "description": "MCP universal (qualquer CLI): filesystem+exec, web sem chave, orquestração de subagentes, DevEngine e automação de navegador (headless + Chrome logado via extensão). Zero deps.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "canivete-mcp": "src/server.mjs"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "serve": "node src/serve.mjs",
    "check": "node --check src/server.mjs && for f in src/lib/*.mjs; do node --check $f || exit 1; done && node --check extension/background.js && node --check extension/content.js && node --check extension/popup.js && node --input-type=module -e \"import fs from 'node:fs';JSON.parse(fs.readFileSync('extension/manifest.json','utf8'));JSON.parse(fs.readFileSync('config/models.json','utf8'))\" && echo OK",
    "doctor": "npm run check && node test/smoke.mjs",
    "smoke": "node test/smoke.mjs",
    "simulate": "node test/simulate.mjs",
    "test": "node test/smoke.mjs",
    "bench": "node test/bench.mjs",
    "bench:local": "node test/bench.mjs --local"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "automation",
    "browser",
    "chrome",
    "cdp",
    "devtools"
  ],
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "example": "examples",
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AsafeTork/canivete.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/AsafeTork/canivete/issues"
  },
  "homepage": "https://github.com/AsafeTork/canivete#readme"
}
